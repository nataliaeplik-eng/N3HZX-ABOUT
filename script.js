const dot=document.querySelector('.cursor-dot');const ring=document.querySelector('.cursor-ring');const lCursor=document.querySelector('.l-cursor');if(dot&&ring){window.addEventListener('mousemove',e=>{dot.style.left=e.clientX+'px';dot.style.top=e.clientY+'px';ring.style.left=e.clientX+'px';ring.style.top=e.clientY+'px';if(lCursor){lCursor.style.left=e.clientX+'px';lCursor.style.top=e.clientY+'px'}});document.querySelectorAll('a,.tile').forEach(el=>{el.addEventListener('mouseenter',()=>{ring.style.width='52px';ring.style.height='52px'});el.addEventListener('mouseleave',()=>{ring.style.width='34px';ring.style.height='34px'})})}const hero=document.querySelector('.hero');if(hero){hero.addEventListener('mousemove',e=>{const r=hero.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;hero.querySelectorAll('.sticker').forEach((el,i)=>{const s=(i+1)*8;el.style.transform=`translate(${x*s}px,${y*s}px) rotate(${i===1?7:-7}deg)`})})}const quote=document.querySelector('.quote-circle');const popup=document.querySelector('.quote-pop');const close=document.querySelector('.quote-close');if(quote){quote.style.pointerEvents='auto';quote.addEventListener('mouseenter',()=>document.body.classList.add('quote-hover'));quote.addEventListener('mouseleave',()=>document.body.classList.remove('quote-hover'));quote.addEventListener('dblclick',e=>{e.preventDefault();if(popup)popup.classList.add('open')})}if(close&&popup)close.addEventListener('click',()=>popup.classList.remove('open'));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&popup)popup.classList.remove('open')});