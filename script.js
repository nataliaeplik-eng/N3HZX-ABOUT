const dot=document.querySelector('.cursor-dot');const ring=document.querySelector('.cursor-ring');if(dot&&ring){window.addEventListener('mousemove',e=>{dot.style.left=e.clientX+'px';dot.style.top=e.clientY+'px';ring.style.left=e.clientX+'px';ring.style.top=e.clientY+'px'});document.querySelectorAll('a,.tile').forEach(el=>{el.addEventListener('mouseenter',()=>{ring.style.width='52px';ring.style.height='52px'});el.addEventListener('mouseleave',()=>{ring.style.width='34px';ring.style.height='34px'})})}const hero=document.querySelector('.hero');if(hero){hero.addEventListener('mousemove',e=>{const r=hero.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;hero.querySelectorAll('.sticker').forEach((el,i)=>{const s=(i+1)*8;el.style.transform=`translate(${x*s}px,${y*s}px) rotate(${i===1?7:-7}deg)`})})}const quote=document.querySelector('.quote-circle');const popup=document.querySelector('.quote-pop');const close=document.querySelector('.quote-close');if(quote&&popup){let clicks=0,timer;quote.addEventListener('click',()=>{clicks++;clearTimeout(timer);timer=setTimeout(()=>clicks=0,420);if(clicks===2){popup.classList.add('open');clicks=0}})}if(close&&popup)close.addEventListener('click',()=>popup.classList.remove('open'));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&popup)popup.classList.remove('open')});